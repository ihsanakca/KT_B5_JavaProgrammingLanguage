Day 19



            /** + Enter --> method description

            Baska bir classin metodunu cagirirken,
            bulundugumuz classda
            ClassAdi.methodIsmi() yazarak cagirabiliriz. (public) (private ise cagiramayiz)

            Method Overloading:

            -Multiple different mmethods sharing the same NAME
            -Same MethodName, Different parameters (parameter number, parameter datatype)
            -Return type, access modifiers, specifier can be same or different

            Advantages:
            1- easy to remember
            2-easy to read (readable)
            3-reusable
            4-flexible

            Tasks:
            //	4. create return  method that can check if a person is eligible to vote
            //    Ex:
            //				eligibleToVote(19, "TR");
            //
            //			output:
            //				You are not eligible to vote!

           //	5. create a method that returns calculate the grade of the student based on the score
           // score >= 90 grade 'A'
           // score >= 80 grade 'B'
           // score >= 70 grade 'C'
           // score >= 60 grade 'D'
           // score < 60 grade 'F'