day 38
        Constructors
            Review
                Wrapper Class:
                    1. parse methods :  converts String to primitive
                    2. valueOf methods : converts String to wrapper

                Data Structures:
                    Array: size is fixed, supports both primitives and non primitives
                    Collection: size is dynamic, doesnot support primitives

                ArrayList : part of the Collection

                ArrayList Methods
                    add(Data)           : adds the data after the last index of the ArrayList
                    add(index, Data)    : inserts the data to the given index
                    size()              : returns the total number of elements
                    get(index)          : returns the element at the given index
                    set(index,Data)     : replaces the element at the given index with the Data
                    remove(int index)   : removes the element at the given index
                    remove(Object)      : removes the given object from arraylist, returns boolean
                    removeAll(Collection): removes the given collection elements from the arraylist
                    clear()             : remove all all elements in the arraylist, size will be 0
                    indexOf(Data)       : returns the first matching  element's index
                    lastIndexOf(Data)   : returns the last matching  element's index
                    contains(Data)      : returns true if the element is in the arraylist otherwise false if not
                    equals(Arraylist)   : returns true if two arraylists are equal (same elemets in same order), otherwise false


                public static void main(String[] args) {
                }

                Ozel bir method
                args arguments kisaltilmisi
                String[] arg
                Bu argumanlar disardan girinlen veriler